name: Unit Tests
on: [push, workflow_dispatch]

jobs:
  test:
    name: Test
    runs-on: macOS-11

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Run Tests
      run: |
        ./scripts/unittests.sh
